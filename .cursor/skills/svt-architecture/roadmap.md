# Дорожная карта миграции СВТ: легаси → BlazorSVT

> Источник: Confluence «Архитектурная трансформация СВТ» (обновление от 04.08.2026).  
> Архитектурные решения: «[Архитектура СВТ: проблемы и решение](reference.md)» 2.0.  
> Скоуп справочников (состав доменов, работы RO/E/L, меню, приоритеты) — «[Реестр справочников СВТ 2.0](catalogs-scope.md)»; план подгоняется под реестр.  
> При синхронизации с Confluence устранены противоречия (сквозная запись (write-through), побеждает последняя запись (LWW), watermark, SoT, права, скоуп).

**Как читать**

- ч. I — для согласования (план, Gate, митигации);
- этапы MVP, оценки трудоёмкости и чеклист работ — [backlog.md](backlog.md);
- детальный расчёт — `docs/estimation/`.

---

# I. План трансформации СВТ

## 1. Контекст и цели

### 1.1. Бизнес-контекст

- СВТ — MDM по управлению логистическими маршрутами, тарифами и складскими ограничениями.
- Масштаб: 101 сущность в легаси; 10–100 полей; до ~2 млн записей (цель новой системы — до 10 млн); пакетная загрузка до 30 тыс. строк.
- **Скоуп переноса:** **63** справочника; **38 вне скоупа**. Состав, работы (RO / RO+E / RO+E+L), меню и приоритеты — в [catalogs-scope.md](catalogs-scope.md) (источник истины; при изменении требований правится реестр, затем план).
- Пользователи: ~300 на чтение, ~10 на запись.
- Интеграции: ~30 потоков.
- Языки UI: ru / en.

### 1.1а. Группы справочников и группы пользователей

Скоуп 63 справочника делится на группы по бизнес-домену. 4 группы пользователей работают
с записью в свои домены независимо (без пересечения по бизнес-функционалу) — это позволяет
переносить запись (редакторы и загрузчики) вертикально по доменам и флипать SoT инкрементально,
домен за доменом (см. §2а). Справочники, которые используются только в одном домене (в т.ч.
бывшие «вспомогательные» ставки), остаются в этом домене, а не выносятся в общий.

Состав каждого домена, сложность, работы и приоритеты — в [catalogs-scope.md](catalogs-scope.md).

| Группа справочников | Кол-во | RO / RO+E / RO+E+L | Группа пользователей | Комментарий |
|---|---|---|---|---|
| Ставки | 20 | 10 / 3 / 7 | A | Максимально нагружены и нестабильны в легаси; влияют на бизнес-планирование компании — переносятся первыми. Заявки (TransportRequest, MassiveRequest) — только RO-грид; workflow — MVP 0.9. Пересчёт AverageRateLevel3 — в волне записи этого домена |
| Маршруты | 12 | 3 / 0 / 9 | B | Независимая группа пользователей. Пилот редактора/загрузчика TransportLegs (MVP 0.5/0.6) **не** равен Gate C этого домена; первый SoT flip — Ставки |
| Продукты | 6 | 2 / 0 / 4 | A (та же, что Ставки) | Ниже приоритет, чем Ставки, хотя те же пользователи |
| Склады и ограничения | 8 | 3 / 0 / 5 | D | Независимая группа пользователей |
| Матрица каналов | 8 | 0 / 0 / 8 | C | Независимая группа пользователей; данные могут частично пересекаться со Складами, но не пользователи. У всех восьми — RO+E+L |
| Интеграция с ЦБД | 4 | 1 / 0 / 3 | A (та же, что Ставки) | Ниже приоритет, чем Ставки и Продукты. CBDRateProducts — таблица/view без модуля |
| Вспомогательные | 5 | 1 / 2 / 2 | A + B + C + D (используют все) | Переносятся последними — нужна обратная связь всех 4 групп. Не «свалка остатка»: только сущности, нужные нескольким доменам |

Итого по работам (все домены, [catalogs-scope.md](catalogs-scope.md)): **20** только RO; **5** RO+E; **38** RO+E+L. Редакторы = RO+E + RO+E+L (**43**); загрузчики = RO+E+L (**38**). Тираж записи — не «всем подряд оба контура».

Этот же список из 7 доменов — единый канонический справочник, используемый одинаково для критериев
Gate A/B/C/D (§2а), доменной ролевой модели («Редактор {Домен}», backlog 0.4.1) и доменного write-toggle
(backlog 0.5.1) — чтобы три механизма не расходились в классификации справочников по доменам.

Порядок переноса (единый для справочников только для чтения (read-only) и для вертикального тиража записи):

**Ставки → Маршруты → Продукты → Склады и ограничения → Матрица каналов → Интеграция с ЦБД → Вспомогательные**

### 1.2. Цель трансформации

- Снизить совокупную стоимость владения (TCO).
- Обеспечить масштабируемость объёмов данных и расчётов.
- Устранить архитектурные ограничения легаси, препятствующие развитию.
- Цель — не «чистая архитектура ради стека».

### 1.2а. Краткий глоссарий терминов

Узкие английские термины ниже пояснены по-русски. В остальном тексте документа
при **первом** упоминании нового термина предпочтительна форма «русский перевод
(English)»; далее допускается короткий английский термин из левой колонки
(без смеси языков внутри одного слова).

| Термин | Пояснение |
|---|---|
| Strangler Fig | эволюционное замещение системы |
| SoT | источник истины (Source of Truth) |
| cutover | момент смены источника истины |
| dual-run / exit dual-run | параллельная работа двух контуров / выход из неё |
| write-through | сквозная запись нового контура в легаси-таблицы |
| write-toggle | переключатель записи |
| watermark / watermark-sync | водяной знак / синхронизация по водяному знаку (по `rowversion`) |
| CQRS | разделение чтения и записи |
| FTS | полнотекстовый поиск |
| circuit | интерактивная сессия Blazor Server |
| app pool | пул приложений IIS |
| sticky session / affinity | привязка сессии к узлу |
| fail-closed | блокировка по умолчанию при сбое |
| kill switch | аварийный выключатель |
| blast radius | радиус поражения инцидента |
| PoC | доказательство концепции |
| UAT | приёмочное тестирование |
| TCO | совокупная стоимость владения |
| RTO/RPO | целевое время восстановления / допустимая потеря данных |
| NFR | нефункциональные требования (Non-Functional Requirements) |
| SCA | анализ состава ПО на уязвимости (Software Composition Analysis) |
| secure SDLC | безопасный жизненный цикл разработки |
| SoD | разделение обязанностей (Separation of Duties) |
| threat model | модель угроз |
| STRIDE | классификация угроз: Spoofing, Tampering, Repudiation, Information disclosure, Denial of service, Elevation of privilege |
| SLI | индикатор уровня сервиса (Service Level Indicator) |
| SLO | цель уровня сервиса (Service Level Objective) |
| pentest | пентест / тест на проникновение |
| expand/contract | расширение схемы → миграция данных → сжатие (совместимые изменения DDL) |
| review/rework | доля времени на ревью кода и переделки |
| append-only | журнал только на добавление (без UPDATE/DELETE) |
| WORM | однократная запись, многократное чтение (Write Once Read Many) |
| ADR | протокол архитектурного решения (Architecture Decision Record) |
| last-write-wins (LWW) | побеждает последняя запись |
| Transactional Outbox | транзакционный исходящий журнал |
| read-only | только для чтения |
| SoT flip | смена источника истины по домену |
| Decision Record | протокол решения (например, SoT Flip Decision Record) |
| rollback drill | репетиция отката |
| residual risk | остаточный (осознанно принятый) риск |
| runbook | операционная инструкция реакции на инцидент |

### 1.3. Стратегия

- **Strangler Fig**: поэтапное замещение, общая БД на переходный период, параллельная работа старой и новой систем.
- **SoT = легаси-таблицы до cutover.**
- **Exit dual-run** — волнами (по справочнику/группе): выключение старого UI после сверки и приёмки; запись по-прежнему write-through в легаси-таблицы.
- **Механизм хостинга и переключения UI**: новое приложение — дочернее приложение в том же IIS-сайте,
  что и легаси (не отдельный сайт), с переиспользованием identity/Integrated Security пула приложений IIS (app pool)
  (без кросс-сайтовой авторизации). Переключение между легаси-UI и новым UI постранично/по домену —
  через IIS URL Rewrite. Это техническая реализация «выключения старого UI» в exit dual-run и Gate B
  (см. §2а).
- **SoT flip — инкрементальный, по доменам** (Gate C, см. §2а): каждый домен (Ставки, Маршруты,
  Продукты, Склады и ограничения, Матрица каналов, Интеграция с ЦБД, Вспомогательные) флипается
  независимо, административным решением, сразу после прохождения им Gate A (сверка) и Gate B
  (ОПЭ) — не дожидаясь готовности остальных доменов.
- **Write-through в легаси продолжается для всех флипнутых доменов без ограничения по времени** —
  вплоть до единого финального демонтажа легаси (Gate D, MVP 2.0). Это даёт возможность отката
  на легаси по любому домену в любой момент до Gate D без потери данных.
- **Gate D (физический демонтаж легаси)** — один раз, глобально, после того как ВСЕ домены
  (включая Вспомогательные) прошли Gate C; необратим.

---

## 2. Архитектурные решения: плюсы, риски, меры

| Решение | Преимущества | Риски | Меры |
|--------|--------------|-------|------|
| Поэтапная замена (Strangler Fig) и общая БД на переходный период; **SoT = легаси-таблицы до cutover**; exit dual-run волнами → **инкрементальный SoT flip по доменам** (Gate C) → единый финальный демонтаж легаси (Gate D, MVP 2.0) | • Низкий риск для бизнеса<br>• Параллельная сверка<br>• Откат по домену в любой момент до Gate D (write-through не прерывается)<br>• Риск финального события снижен — доменов много, необратимое событие одно (Gate D) | • Долгая поддержка обоих контуров (dual-run)<br>• Постоянный write-through — нагрузка на легаси вплоть до самого конца проекта<br>• Пользовательский dual-run (UI) короткий и по домену (до Gate B, ~6 недель); инфраструктурный dual-run (write-through, легаси-инфраструктура) — длится до глобального Gate D (весь срок программы) | • Формальные критерии Gate A/B/C/D на каждый домен (см. §2а)<br>• Не более одного домена одновременно в Gate B (ОПЭ) — ограничение по команде (2 разработчика); Gate A других доменов может идти параллельно<br>• Протокол «SoT Flip Decision Record» на каждый домен<br>• Gate D: план «новые объекты → переключение → удаление легаси-артефактов»<br>• Gate C+ (опционально): точечная ретирация write-through для исключительно-доменных таблиц раньше глобального Gate D (см. §2а)<br>• Адаптивная длительность Gate B: минимум 2 недели при устойчивых автоматических сигналах, иначе — 4 недели (см. §2а)<br>• Обязательная репетиция отката (rollback drill) на первом флипнувшем домене («Ставки», MVP 0.7)<br>• Открытый вопрос (решение за бизнесом): расширение команды на этапе 0.7 (тираж записи) для распараллеливания Gate B по доменам сократило бы суммарную длительность dual-run ценой доп. штата |
| Модульный монолит и Blazor Server без отдельного фронтенда | • Быстрее малой командой<br>• Один стек .NET<br>• Проще поставка и отладка | • Рост связанности модулей — единая сборка `BlazorSvt.csproj`, граница `Modules/{X}` держится тестом, а не компилятором (остаточный риск при росте команды/параллельных потоков разработки; сейчас — 1 разработчик + AI-агент по единому скиллу, нарушений на момент проверки нет)<br>• Рост числа одновременных пользователей упирается в память/CPU сервера и привязку сессии к узлу | • Dependency-тест границ модулей в CI (`tests/BlazorSvt.UnitTests`, NetArchTest/reflection): `Modules.X` не ссылается на `Modules.Y` при X≠Y; `Platform` не ссылается на `Modules.*` — см. 0.2.2.6; формализовано как жёсткое нарушение в `svt-development-patterns.mdc`<br>• Запрет связей между модулями, только через платформу<br>• Оценка ёмкости: сколько ресурсов расходуется на всех пользователей<br>• Алерты по памяти, числу сессий, обрывам<br>• Балансировка с привязкой сессии к узлу<br>• При нехватке ёмкости: усиление серверов или отдельное решение по архитектуре хостинга<br>• Если дисциплина не удержится при тираже ~55+ модулей — рассмотреть раздельные `.csproj` на модуль (internal + InternalsVisibleTo) как более жёсткую compile-time границу (сейчас сознательно отложено) |
| MS SQL как единственное хранилище и поиск (FTS); отказ от Elastic | • Нет рассинхрона кеша<br>• Меньше точек отказа<br>• На ставках и средневзвешенных ~10× устойчивость к отказу относительно легаси | • Сложный поиск хуже, чем в отдельном поисковом движке<br>• Нагрузка на SQL | • Ранний нагрузочный PoC сразу после MVP 0.1 на объёме, близком к целевому (10 млн), не дожидаясь полного тиража 63 справочников (см. [backlog.md](backlog.md) 0.2.1)<br>• Индексы и каталоги FTS<br>• Мониторинг ресурсов, мониторинг сложных запросов<br>• Алерты по памяти, процессору<br>• Resource Governor: изоляция ресурсов воркера синхронизации и пакетной загрузки от интерактивных grid-запросов<br>• Троттлинг батч-загрузчика (подпачками, не одной серией на 30 тыс. строк)<br>• Партиционирование по периоду (год) для крупных архивных таблиц при приближении к 10 млн (см. §2 строка sync, retention policy)<br>• **Предварительно подтверждено на MVP 0.1 (см. [backlog.md](backlog.md) 0.2.1):** 200К изменённых записей → sync за 30 сек, SQL Server (4 ядра) — CPU ≈15% на время цикла, сервер приложений — прирост <1%; полный вывод — после совместного прогона 0.2.1.10 на целевом объёме |
| Типизированные таблицы вместо метамодели; скоуп 63 справочника (38 неиспользуемых вне скоупа) | • Ограничения целостности<br>• Проще доработки<br>• Выше устойчивость данных | • Больше работы на каждый справочник при тираже | • Шаблоны SQL/C#<br>• Навыки ИИ-агента<br>• Вертикальный срез «один справочник = один модуль» |
| Судьба 38 справочников вне скоупа: не переносятся в новую систему; легаси остаётся единственным источником для них вплоть до Gate D | • Не тратим ресурс разработки на данные, не используемые бизнес-процессами<br>• Не размывает границы Gate D дополнительными исключениями | • Потеря доступа к историческим данным после физического демонтажа легаси (Gate D)<br>• Ранняя выгрузка устареет — легаси остаётся SoT и продолжает функционировать для этих таблиц до самого демонтажа | • Экспорт всех 38 в Excel → размещение на Confluence непосредственно перед Gate D (backlog 2.0.1.2), силами техподдержки, не разработки; группировка файлов — по доменам §1.1а<br>• Доступ к страницам Confluence — по тем же AD-группам, что и текущий легаси-доступ на чтение (без расширения экспозиции данных)<br>• Проверка объёма данных против лимита листа Excel (~1 048 576 строк) — заранее, на пункте 0.2.2.1, а не постфактум перед Gate D<br>• Штатное полное резервное копирование (full backup) БД перед выводом легаси из эксплуатации — бесплатная страховка на случай неполноты ручной выгрузки (подтвердить у ИТ, что retention покрывает состояние на момент отключения)<br>• Обновить `docs/user-guide` при Gate D: заменить указание «справочники, которые ещё не перенесены → Старая СВТ» на ссылку на архив в Confluence |
| Бизнес-логика в C#, не в процедурах и SQL Agent | • Проще тесты и сопровождение<br>• Расчёты масштабируются на сервере приложений | • Утечка логики в SQL<br>• Нехватка CPU/RAM приложения | • Запрет бизнес-правил в процедурах (только чтение/сохранение)<br>• Оценка ёмкости серверов<br>• Мониторинг ресурсов |
| Разделение чтения и записи (CQRS): грид из снимка (snapshot), карточка из проекции легаси | • Быстрые операции фильтрации, сортировки, пагинации в гриде на больших объемах<br>• Актуализация карточки в момент записи (SoT = легаси) | • Задержка актуализации грида относительно карточки (до интервала воркера) | • Грид обновляется только через воркер синхронизации (одна точка обновления snapshot)<br>• Опционально позже: при Save триггерить воркер, если лаг неприемлем для бизнеса (бэклог) |
| Прямая синхронизация легаси → грид: водяной знак (watermark) по версии строки (rowversion) | • Инкремент без брокера<br>• Повтор безопасен<br>• Отказ одного справочника не останавливает остальные | • Отставание грида<br>• Жёсткие удаления обнаруживаются только сверкой (не сразу); физическая логика удаления пока в легаси SQL Agent, не в C# | • Воркер бэкенда: инкремент раз в N сек (по умолчанию 90, настраивается); курсор после успешной записи; граница цикла = `MIN_ACTIVE_ROWVERSION() - 1` (не `MAX(rowversion)`) — исключает пропуск изменения из-за транзакции, закоммиченной позже с меньшим rowversion<br>• Оперативный выключатель синхронизации: справочник переключателей фич легаси (`V2SyncEnabled`)<br>• Логи/алерты отставания курсора; алерт на отказ/невыполнение самого цикла сверки удалений<br>• Суточная сверка удалений (reconciliation), ограничена партицией `IsArchive = 1` (partition elimination, дешевле при 10 млн записей)<br>• Автосверка по кодам раз в час (настраиваемо): при расхождении, если изменение в легаси старше 2 мин → письмо в поддержку<br>• Бизнес-обоснование допуска: жёсткое удаление — только для уже архивных/просроченных записей с согласованным для каждого справочника лагом (например, 2 года для ставок) → фантом в пределах окна reconciliation — согласованный, не критичный риск (запись и так помечена как неактуальная)<br>• Retention policy (для каждого справочника: лаг, поле отсчёта даты) — конфигурация в Platform; инвентаризация легаси заданий жёсткого удаления — см. 0.2.2.5<br>• Перенос физической логики жёсткого удаления из легаси SQL Agent в C# — по домену, при появлении конвейера записи CQRS (см. 0.7.2.76 / 0.8.2.8), с периодом параллельной проверки перед отключением легаси job |
| Обратная запись: напрямую в легаси-таблицы (write-through); команды идемпотентны; SoT = легаси до cutover | • Карточка сразу актуальна<br>• Без очереди и брокера на master-данные<br>• Безопасный повтор сохранения и пакета без дублей | • Жёсткая связь с моделью легаси<br>• Ошибка SQL = ошибка сохранения<br>• Нужен стабильный ключ повторного применения<br>• Единый глобальный write-toggle — широкий радиус поражения (blast radius): инцидент в одном домене вынуждает гасить запись всем доменам, включая уже стабильные/флипнутые | • Шаг persist пайплайна CQRS пишет в легаси-таблицы в одной транзакции<br>• Понятная ошибка и повтор<br>• Доменный write-toggle по образцу уже работающего `V2SyncFeatureToggle`/`V2SyncEnabled`: деплойный `Write:Enabled` (все домены, требует рестарта, аварийный рубильник) + оперативный `V2WriteEnabled_{Домен}` в `vw_FeatureToggle` (fail-closed по конкретному домену, без рестарта, не задевает остальные домены); один и тот же toggle домена — для редактора и загрузчика<br>• Проверка toggle на шаге persist, после validate (пользователь видит ошибки валидации, даже если запись временно недоступна)<br>• Параметризация доменом закладывается в CQRS-ядро сразу в MVP 0.5 (backlog 0.5), не откладывается до тиража по доменам (backlog 0.7)<br>• Идемпотентность в конвейере CQRS с пилота редактора |
| Конкуренция записей: побеждает последний (как в легаси), без блокировок | • Паритет с As-Is<br>• Проще реализация | • Редкая перезапись чужих правок при двух UI | • Принятый риск: пользователи почти всегда на разных записях<br>• После MVP 1.0 аудит «кто/когда» для разбора |
| Один конвейер для редактора и загрузки Excel | • Одни правила<br>• Нет расхождения ручного ввода и пакета | • Сложный общий каркас | • Общие проверки и команды<br>• Партиальный успех батча: валидация всех N строк в стейджинге без коммита → пользователь выбирает «отменить всё» или «сохранить валидные» → коммитятся только валидные M ≤ N строк (каждая строка — независимая команда persist, паритет с принципом «last-write-wins без блокировок»); по невалидным (N−M) формируется скачиваемый отчёт об ошибках (Excel в структуре исходного файла + колонка(и) с описанием ошибки по строке/полю) для правки и повторной загрузки уже малого поднабора, а не всех 30 тыс. заново |
| Отказ от брокера сообщений в целевом контуре; внешние адаптеры не переписываем, оркестрацию выносим из SQL Agent позже | • Меньше распределённости<br>• Сохранение внешних контрактов | • Сбои потоков интеграций без готовой шины | • Отчёты по ошибкам потоков<br>• Повторы и идемпотентность в прикладном контуре<br>• Отдельный этап модернизации интеграций |
| ИИ-агенты и навыки переноса из метаданных легаси | • Ускорение тиража типовых справочников | • Нестандартные правила<br>• Завышение плана | • Пилоты<br>• Ручная доработка уникального<br>• Оценка по сложности правил<br>• Калибровка раздельно по категориям работ (не единый коэффициент на весь план): (а) тираж read-only по фреймворку (backlog 0.2) — подтверждено фактом: 2 модели ИИ + факт после выполнения (разброс моделей ~10%); простой справочник (тип «Узлы») ≈ 2 ч, сложный (тип «Ср. ставки») ≈ 1 день; (б) тестирование/стабилизация — не подтверждено, оставлено консервативным; (в) ОПЭ/календарное время Gate — не ускоряется ИИ в принципе, это критический путь программы, считается отдельно (см. [backlog.md](backlog.md), MVP 0.7/0.8); (г) бизнес-логика контура записи (backlog 0.5–0.8) — отдельная, всё ещё не откалиброванная фактом категория, калибруется по пилотам 0.5/0.6/первому домену 0.7, не наследует калибровку (а)<br>• Формальный процесс «2 модели ИИ + факт» — обязателен для каждого будущего пилота/партии тиража (см. 0.5/0.6/0.7.2)<br>• Независимое (не от Ивана, не от ИИ-модели) подтверждение от бизнеса на точечных примерах отчётов (см. reference.md §4.5): архитектурный эффект (легаси → новый контур, без учёта ИИ) — ускорение разработки в 2–3 раза; это точечные примеры, не по методике «2 модели + факт», но полезны тем, что независимо разделяют эффект архитектуры и эффект ИИ-инструментов — именно то разделение, которого требует architecture review |
| Роли и сопоставление с группами AD; доступ к БД — Integrated Security, общая учётка управляемой службы (MSA) IIS app pool (унаследовано от легаси для всех пулов, согласовано с ИБ ранее) | • Контролируемый доступ на проде<br>• Переиспользование уже одобренного ИБ механизма, не новое согласование с нуля | • Дыры в правах до полной модели<br>• Легаси-модель рассчитана на 1 домен (согласована с ИБ в 2019 г.), не на 7 доменов текущего скоупа — роль «Единое окно» даёт кросс-доменную запись<br>• Разграничения на уровне БД нет (единая учётка) → прикладной слой (проверка прав) — единственный, а не один из, рубеж контроля доступа | • До MVP 0.4 включительно: только чтение всех справочников и отчётов, если у пользователя есть хотя бы одна роль из `dbo.Role` (связка `User` ← `UserRole` → `Role`, группы AD в виде `DOMAIN\Group`); тот же контингент, что уже имеет доступ к легаси; отдельное согласование с ИБ не требуется<br>• MVP 0.4: доменная ролевая модель — «Редактор {Домен}» 1:1 с группами §1.1а, отдельная роль «Редактор вспомогательных» (шире blast radius, отдельное согласование), отдельное кросс-доменное право «оператор заявок», «Админ» остаётся глобальным суперюзером; формальное согласование с ИБ — обязательная веха перед MVP 0.5 (см. backlog 0.4.1)<br>• Осознанный отказ от раздельных read/write SQL-логинов: вся авторизация — в C#, не в правах БД (соответствует принципу «бизнес-логика в C#»)<br>• Dev-секрет БД — только в изолированной dev-сети, вне scope прод-рисков; тест/прод — без секрета в конфиге (Integrated Security) |
| Наблюдаемость, сверка данных, аудит изменений | • Быстрое расследование<br>• Доверие к двойному контуру | • До MVP 1.0 нет аудита на уровне полей (риск принят)<br>• Расхождения dual-run<br>• Наивная асинхронная запись аудита (без durable-стадии) может тихо терять записи при падении/рестарте процесса между `persist` и отложенной записью — подрывает саму цель аудита | • Сквозные идентификаторы в логах<br>• Автосверка по кодам (см. строку sync)<br>• Аудит полей — с MVP 1.0; до этого риск отсутствия аудита принят<br>• Аудит — Transactional Outbox (см. backlog 1.0.1): durable-стадия (1 компактная строка на изменённую запись) синхронно в транзакции persist + асинхронное разворачивание в пофайловую проекцию отдельным `BackgroundService` по образцу `SnapshotSyncScheduler`, без брокера сообщений (см. строку «Отказ от брокера») |
| RTO/RPO/доступность — унаследованы от инфраструктуры легаси (общая ВМ и общая БД, не отдельный DR-контур для v2) | • Не нужно проектировать/содержать отдельную DR-инфраструктуру для новой системы<br>• Restore легаси-таблиц и `v2.*` (snapshot/`SyncState`/будущий outbox аудита) атомарно консистентен «бесплатно» — общая БД восстанавливается целиком на одну точку времени, без отдельной сверки двух хранилищ после DR | • RPO сейчас — сутки (суточный инкрементальный бэкап); план — час, после проверки влияния на производительность при целевом объёме (не при текущем)<br>• RTO сейчас — факт 3ч, цель 1ч; зависит от смежной команды (инфраструктура/техподдержка), не от разработки BlazorSVT — риск не «нет цели», а «нет формального согласования»<br>• Доступность по функциональному контракту с бизнесом — 98,5% (обслуживается техподдержкой; недоступность — плановые работы, общие сбои инфраструктуры/сети, инциденты внутри системы); контракт не меняется, но новый прикладной контур добавляет собственные, не унаследованные сценарии недоступности (см. §2.1) | • Явно зафиксировано: RTO/RPO/доступность новой системы = RTO/RPO/доступность легаси по конструкции (общая ВМ/БД), отдельный DR-контур для v2-схемы не проектируется<br>• Проверка влияния часового бэкапа на производительность — сценарий в раннем нагрузочном PoC на целевом объёме (см. [backlog.md](backlog.md) 0.2.1)<br>• Формальное согласование целевых RTO/RPO с командой инфраструктуры/поддержки — часть подтверждения готовности ПРОД (0.5.5/0.5.6), не блокирующий критерий: если не согласовано к моменту релиза — риск явно фиксируется как принятый<br>• Новые сценарии недоступности прикладного слоя — явный перечень и митигации в §2.1, не молчаливое допущение |
| Заявки на расчёт ставки: отдельный модуль, простой workflow (не BPM) | • Соответствие текущим бизнес-потребностям<br>• Без тяжёлого BPM | • Другая модель, чем справочники<br>• Доработка процесса после запуска | • Детальная аналитика до пилота<br>• Тот же CQRS и платформа справочников где возможно |
| Дашборды: фиксированные сценарии в коде, кеш-таблицы, без универсального конструктора | • Быстрый отклик<br>• Меньше стоимости сопровождения<br>• Контроль логики агрегатов | • Нет произвольного (ad-hoc) конструктора для бизнеса<br>• Устаревание кеша | • ~10 согласованных редко изменяемых дашбордов<br>• Признак актуальности кеша<br>• Переход с детализацией (drill-down) в существующий грид<br>• `ReportConstructor` (Конструктор отчётов ставок) — **не** дашборд: справочник наборов фильтров + отчёт Excel из архива (MVP 0.3, backlog 0.3.1.5) |

**Порядок переноса справочников (группы):**  
Ставки → Маршруты → Продукты → Склады и ограничения → Матрица каналов → Интеграция с ЦБД → Вспомогательные.

### 2.1. Новые (не унаследованные от легаси-инфраструктуры) сценарии недоступности

Контракт доступности 98,5% (см. строку «RTO/RPO» выше) наследуется от общей ВМ/IIS/БД и
покрывает **инфраструктурные** причины простоя (сеть, плановые работы, отказ ВМ) — они
действительно не меняются. Но новый прикладной контур (Blazor Server + watermark-sync + write-toggle) добавляет сценарии недоступности/деградации, которых у легаси не существовало
физически, и которые контракт 98,5% сам по себе не покрывает — они митигируются отдельно:

| Новый сценарий | Почему его не было в легаси | Митигация |
|---|---|---|
| Перезапуск пула приложений IIS (app pool recycle) обрывает все активные интерактивные сессии Blazor (circuit) (пользователь видит «reconnecting…», может потерять несохранённый ввод) | В легаси (Angular + REST) перезапуск пула приложений IIS (app pool) прозрачен — следующий запрос просто чуть медленнее | Явно зафиксировано как известный компромисс Blazor Server; смягчается редким релизным окном (вне рабочего времени) |
| Отказ/зависание воркера синхронизации — грид отстаёт от легаси без явной ошибки для пользователя | Отдельного sync-процесса в легаси не было (одна БД) | `V2SyncEnabled` (kill switch, 0.1.7.5) + алерты 0.1.7.6/0.1.7.7 + подключение алертов к техподдержке (0.1.7.8) |
| Стопор доменного write-toggle из-за бага нового кода — блокирует запись по конкретному домену | В легаси такой единой точки отказа для записи по домену не было | Доменный (не глобальный) `V2WriteEnabled_{Домен}` — ограничивает blast radius одним доменом (§2, строка «Обратная запись») |
| Некорректная конфигурация IIS URL Rewrite при переключении легаси-UI ↔ новый UI в период dual-run | Механизма переключения UI не существовало — был один UI | Обязательная проверка URL Rewrite перед первым Gate B каждого домена (2.7) |

Ни один из этих сценариев не отменяет наследование 98,5% на уровне инфраструктуры — они
дополняют его как явно поднятые и отдельно смягчаемые риски нового прикладного слоя, а не как
молчаливое допущение «то же самое, что и раньше».

---

## 2а. Критерии перехода между стадиями SoT (Gate A/B/C/D)

Единица перехода — домен из §1.1а (Ставки, Маршруты, Продукты, Склады и ограничения,
Матрица каналов, Интеграция с ЦБД, Вспомогательные). Флип выполняется независимо по каждому
домену; Вспомогательные — последними, т.к. используются всеми 4 группами пользователей.

**Gate A — техническая сверка домена.** Не требует разработчика; может идти параллельно
у нескольких доменов одновременно (в т.ч. параллельно с Gate B другого домена).

- 0 расхождений категории «потеря/искажение данных» и ≤0,1% расхождений категории
  «ожидаемый lag» за последние 14 календарных дней подряд (автосверка по кодам, см. §2, строка sync).
  Задержка отражения жёсткого удаления архивной/просроченной записи в пределах окна
  reconciliation относится к категории «ожидаемый lag», не к «потере данных» (см. §2, строка sync).
- p95 отставания курсора watermark ≤ 5 минут за период наблюдения.
- Нагрузочный тест на фактическом объёме домена: p95/p99 не хуже легаси.
- Нет открытых дефектов уровня critical/blocker по домену за последние 14 дней.

**Gate B — ОПЭ (опытно-промышленная эксплуатация) домена.** Нужен дежурный разработчик —
не более одного домена одновременно в Gate B; Gate A других доменов может идти параллельно.

- Длительность работы пользователей домена исключительно в новом UI (легаси-UI домена отключён —
  технически через IIS URL Rewrite, см. §1.3; write-through в легаси продолжается) — адаптивная:
  по умолчанию 4 недели; может быть сокращена
  до минимум 2 недель, если за истёкший период 0 расхождений категории «потеря данных» по
  автосверке и reconciliation (см. §2, строка sync) и 0 инцидентов P1/P2 — иначе остаётся 4 недели
  (продление сверх 4 недель — по решению того же состава, что подписывает Gate C, при наличии
  открытых вопросов).
- 0 инцидентов P1 (потеря/искажение мастер-данных), ≤2 инцидентов P2 за период, все закрыты
  с зафиксированной причиной, без повторов.
- Для доменов с расчётами (Ставки, позже — заявки) — сверка итоговых сумм с легаси в пределах
  допустимой погрешности, согласованной с бизнес-владельцем.
- Приёмка ОПЭ подписана бизнес-владельцем домена (для Вспомогательных — представителями
  всех 4 групп пользователей).

**Gate C — SoT flip домена.** Административное решение, обратимое.

- Триггер: Gate B пройден для домена.
- Действие: новый контур официально объявляется источником истины для домена; легаси-UI
  домена остаётся выключенным (уже сделано в Gate B); write-through в легаси-таблицы
  ПРОДОЛЖАЕТСЯ без изменений.
- Оформляется протоколом «SoT Flip Decision Record»: дата, домен, кто подписал (архитектор +
  владелец продукта домена + представитель эксплуатации), ссылка на результаты Gate A/B.
- Rollback: в любой момент до Gate D — реактивация легаси-UI и признание легаси источником
  истины для домена по решению того же состава; данные не теряются, т.к. легаси не отставал
  (write-through не прерывался).

**Gate C+ — точечная ретирация write-through (опционально, до глобального Gate D).** Применимо
только к таблицам/справочникам, исключительным для одного домена (не используются другими,
ещё не флипнувшими доменами — проверяется по перечню общих источников синхронизации и
каскадным FK, см. backlog 0.1.7). Не применимо к общим сущностям (узлы/регионы/страны и т.п.,
группа «Вспомогательные»), которые обслуживают несколько доменов одновременно.

- Триггер: Gate C пройден для домена; прошёл период наблюдения без инцидентов P1 (ориентир —
  не менее 6 месяцев после Gate C, согласуется индивидуально с бизнес-владельцем и архитектором).
- Действие: остановка write-through только для исключительно-доменных таблиц; связанные
  легаси-объекты выводятся из эксплуатации отдельно от Gate D.
- Решение принимается индивидуально по домену/набору таблиц, не является обязательным шагом —
  применяется там, где нужно ощутимо сократить суммарную стоимость/длительность dual-run.
- Не отменяет и не ускоряет Gate D: общие сущности и общая легаси-инфраструктура
  (Front/GOD-сервис/Elastic/RabbitMQ), обслуживающие ещё не флипнувшие домены, продолжают
  жить до Gate D.

**Gate D — финальный демонтаж легаси.** Один раз, глобально, MVP 2.0, необратимо.

- Триггер: ВСЕ домены, включая Вспомогательные, прошли Gate C.
- Действие: остановка write-through, рефакторинг и удаление легаси-артефактов, вывод
  Front/Elastic/RabbitMQ/GOD-сервиса из эксплуатации (см. backlog 2.0.1).
- Rollback не предусмотрен — единственная необратимая точка всего плана.

**Пилоты (MVP 0.5/0.6) через Gate A/B/C не проходят** — их задача проверить фреймворк на одной
сущности («Транспортные плечи»), а не флипнуть домен. Флип домена «Маршруты» (включая
пилотную сущность) происходит позже, в MVP 0.7, вместе с остальными справочниками группы (12 в домене, из них TransportLegs — пилот фреймворка, не Gate C).

### Приёмка функции вне реестра справочников

Read-only справочник из [catalogs-scope.md](catalogs-scope.md) принимается вертикальным срезом и Gate домена выше. Функция, которой в реестре нет (заявки на расчёт ставки — MVP 0.9, аудит изменений — MVP 1.0, дашборды — MVP 1.1 и последующие того же рода), до пункта в [backlog.md](backlog.md) фиксируется так:

- Потребность проверяема: что меняется в работе и по какому наблюдаемому результату это видно.
- Описано, что делает система. Стек, хостинг и границы модулей берутся из [reference.md](reference.md) и §2.
- Функция прослеживается к породившей её потребности; после заведения пункта — к его id в бэклоге.
- Неизвестное (метрика, состав интеграции, класс данных) остаётся явным пробелом. Приёмку закрывает подтверждённое значение.

---

## 2б. Отложенная митигация рисков (условия условного архитектурного одобрения)

**Статус согласования, на которое претендует документация:** условное одобрение
направления трансформации и программы пилотирования — **не** безусловное закрытие
всех нефункциональных требований (NFR) и не разрешение массового тиража / контура записи «как есть».

**Принцип.** Жёлтые зоны повторного архитектурного ревью (architecture review)
(`docs/architecture/review-current-state.html`, условия 3–7) — не скрытые пробелы,
а **осознанный остаточный риск (residual risk)** с владельцем, сроком (Gate / MVP) и проверяемым
критерием «стало зелёным». Документация не выдаёт план за факт: закрытие условия —
либо **артефакт** (анализ/согласование), либо **исполнение** (код/CI/замер/drill).

Краткая формулировка для пакета ревью — `reference.md` §6.9.
Полный текст документных митигаций (тип A) — **приложения ниже в этом же §2б**
(не отдельные файлы): удобнее для человека-ревьюера.

**Что одобряется сейчас (при принятии условий ниже):**
- направление: модульный монолит Blazor Server + MS SQL FTS, поэтапная замена (Strangler Fig), CQRS,
  SoT = легаси до cutover, Gate A/B/C/D;
- продолжение read-only dual-run и развитие каркаса в рамках уже сделанного MVP 0.1;
- план работ и реестр отложенных митигаций как обязательная часть программы.

**Что явно не одобряется «вперёд» этим же решением:**
- старт массового тиража оставшихся read-only (backlog 0.2 / MVP 0.2; 5 пилотов уже на проде, на тираж — 58 из 63) — до закрытия D-PERF
  (минимум 0.2.1.10) и D-ARCH (0.2.2.6);
- пилот записи / загрузчик (MVP 0.5–0.6) — до закрытия D-SEC-RO (0.2.2.7) и наличия
  черновиков D-SEC-TM / D-SEC-SOD / D-DATA-MIG (приложения §2б; формальное согласование ИБ —
  по D-SEC-RO и вехе раздела 9);
- единый коэффициент ускорения ИИ в бюджете программы — до калибровки 0.0.5 (D-EST).

### Классификация работ

| Тип | Смысл | Как «зеленеет» для ревью |
|-----|--------|---------------------------|
| **A** — анализ / согласование / артефакт | Можно закрыть без продакшен-кода или с минимальным прототипом | Появился проверяемый текст (приложение §2б) + (где нужно) подпись/письмо |
| **E** — исполнение / факт | Нужны код, CI, прогон, замер, drill | Чеклист бэклога выполнен + результат зафиксирован |

### Реестр отложенных митигаций

| ID | Условие ревью | Пробел (честно) | Тип | Митигация / артефакт | Закрыть до | Критерий «зелёным» | Блокирует согласование направления сейчас? |
|----|---------------|-----------------|-----|----------------------|------------|--------------------|---------------------------------------------|
| D-PERF | №3 | Нет полного протокола 0.2.1; нет 0.2.1.10 (чтение+запись+sync); 10 млн — однопользовательский замер | E | Выполнить 0.2.1.1–0.2.1.15; заполнить [приложение D-PERF](#приложение-d-perf--протокол-нагрузочного-poc-69) | Старт 0.2.3 (MVP 0.2) | Протокол подписан командой; 0.2.1.10 пройден или зафиксирован принятый риск с компенсирующими мерами | Нет (блокирует тираж) |
| D-ARCH | №4 | Dependency-тест границ модулей написан (NetArchTest, ModuleBoundaryTests), в CI не включён | E | 0.2.2.6 + жёсткий отказ (hard fail) в CI | Старт 0.2.3 | Тест красный при нарушении границ; пайплайн падает | Нет (блокирует тираж) |
| D-OPS | №5 | Алерты sync/ёмкости не подключены к техподдержке | E | 0.1.7.6 / 0.1.7.7 / 0.1.7.8 | До массового dual-run read-only на многих справочниках; целевой — до завершения MVP 0.2 | Алерты видны в инструменте поддержки; есть операционная инструкция (runbook) эскалации | Нет (блокирует зрелость эксплуатации) |
| D-SEC-RO | №5 | Нет письменного подтверждения ИБ по read-only | A | 0.2.2.7 | До старта 0.2.3 | Письмо/протокол ИБ получено | Нет (блокирует тираж) |
| D-SEC-SCA | №5 | Анализ состава ПО на уязвимости (SCA) в CI заявлено, не настроено | E | 0.1.4.6 | До MVP 0.5 (желательно раньше, в рамках безопасного жизненного цикла разработки (secure SDLC)) | Job SCA в GitLab; политика по Critical/High зафиксирована | Нет |
| D-SEC-TM | №5 | Модель угроз (threat model) не описана | A | [Приложение D-SEC-TM](#приложение-d-sec-tm--модель-угроз-threat-model) (классификация STRIDE по контурам) | Черновик — до MVP 0.4; согласование с ИБ — до MVP 0.5 | Документ + список угроз/контролей; открытые угрозы с owner | Нет (усиливает условие №5) |
| D-SEC-SOD | №5 | Разделение обязанностей (SoD), аудит привилегированных действий, неизменность журнала не раскрыты | A (+E позже) | [Приложение D-SEC-SOD](#приложение-d-sec-sod--разделение-обязанностей-sod); реализация аудита/иммутабельности — с MVP 1.0 (outbox) | Матрица SoD — до MVP 0.4; иммутабельность журнала — MVP 1.0 | Матрица «роль × действие» + запрещённые совмещения; решение по хранению аудита зафиксировано | Нет |
| D-SEC-PT | №5 | Тест на проникновение (pentest) не в плане явно | A | 0.5.8.1 (внешний/внутренний pentest контура записи) | До первого Gate B домена с записью (MVP 0.7) или по требованию ИБ раньше | Отчёт pentest; Critical закрыты или приняты письменно | Нет |
| D-OBS | №5 | Нет системного каркаса индикаторов/целей уровня сервиса (SLI/SLO) на всё приложение (есть точечно по sync) | A | [Приложение D-OBS](#приложение-d-obs--индикаторы-и-цели-уровня-сервиса-slislo) | До MVP 0.5 | Таблица SLI/SLO + источник метрики + алерт | Нет |
| D-DATA-MIG | №5 / развёртывание | Совместимые миграции схемы и откат данных для write не описаны | A | [Приложение D-DATA-MIG](#приложение-d-data-mig--совместимые-миграции-схемы-и-откат) (расширение/сжатие схемы (expand/contract)) | До старта MVP 0.5 | Текст принят командой; чеклист на каждый релиз write | Нет (блокирует пилот записи) |
| D-EST | №6 | Калибровка 0.0.5 — план, не факт; нет итоговой цифры плана по категориям | E | 0.0.5.1–0.0.5.3; не смешивать с точечными примерами бизнеса (reference.md §4.5) | После пилотов 0.5–0.7 (минимум 3 категории) | Таблица план/факт по категориям; пересчёт таблицы этапов в [backlog.md](backlog.md) | Нет (блокирует «бюджетный» прогноз с ИИ) |
| D-MET | №7 | Нет явных показателей доли ревью и переделок (review/rework) в 4 метриках пилота | A + E | [Приложение D-MET](#приложение-d-met--метрики-продуктивности-пилотов) + сбор на пилотах 0.5–0.7 (0.0.5.4) | Пилот 0.5 (первый замер) | На каждом пилоте: время человека, время агента, % review/rework, дефекты после приёмки | Нет |
| D-DRILL | Реестр рисков | Репетиция отката (rollback drill) только в плане | E | Rollback drill на первом флипнувшем домене («Ставки», MVP 0.7) — см. §2 | Первый Gate C домена «Ставки» | Протокол drill: шаги, время, результат | Нет |
| D-DUAL | Реестр рисков | Длительный инфраструктурный dual-run | A | Оценка целесообразности Gate C+ (стоимость dual-run vs риск ранней ретирации write-through) | После первого успешного Gate C + ≥6 мес. наблюдения (см. §2а) | Decision Record: применять / не применять C+ | Нет |

### Связь с бэклогом (не дублировать чеклисты)

| ID митигации | Существующие / новые пункты |
|--------------|-----------------------------|
| D-PERF | 0.2.1.1–0.2.1.15 + приложение D-PERF |
| D-ARCH | 0.2.2.6 |
| D-OPS | 0.1.7.6, 0.1.7.7, 0.1.7.8, 0.1.7.11 |
| D-SEC-RO | 0.2.2.7 |
| D-SEC-SCA | 0.1.4.6 |
| D-SEC-PT | 0.5.8.1 |
| D-EST | 0.0.5.1–0.0.5.3 |
| D-MET | 0.0.5.4 + приложение D-MET |
| D-DRILL | §2, строка Strangler Fig (rollback drill) |
| D-SEC-TM / D-SEC-SOD / D-OBS / D-DATA-MIG | приложения ниже; 0.0.6.7; backlog 0.4.1 / 0.5.1 / 1.0.1 при реализации |

### Правило обновления статуса

При закрытии митигации: в таблице выше меняется формулировка пробела на «закрыто (дата, ссылка
на приложение/протокол)» и обновляется зеркало в `docs/architecture/output-review-package/`.
Повторный architecture review должен видеть трассировку: условие HTML ↔ ID D-* ↔ Gate ↔ приложение §2б.

---

### Приложение D-SEC-TM — модель угроз (threat model)

> Статус: **черновик** (митигация D-SEC-TM).  
> Цель: закрыть пробел «threat model не описана» до MVP 0.4; согласование с ИБ — до MVP 0.5
> (вместе с доменной ролевой моделью, backlog 0.4.1).

#### Границы системы

| В scope | Вне scope текущей программы |
|---------|------------------------------|
| Blazor Server UI (дочернее приложение IIS) | Контейнеризация / K8s (диаграмма 03) |
| Watermark-sync worker (легаси → `v2` snapshot) | Переписывание внешних WCF/адаптеров |
| Write-through в легаси-таблицы (с MVP 0.5) | Отдельный DR-контур для v2 |
| Excel batch import (с MVP 0.6) | Полная замена интеграционной оркестрации до MVP 1.2 |
| AD-группы / доменные роли (прикладной слой) | Раздельные SQL-логины read/write |

**Активы:** мастер-данные тарифов/маршрутов/ограничений; snapshot грида; feature toggles;
журналы/аудит (с MVP 1.0); учётные данные сессий пользователей.

**Классификация данных:** признаки коммерческой тайны и персональных данных по этим активам не проставлены (пробел T6). Значение появляется после решения владельца продукта и ИБ.

**Границы доверия:** браузер пользователя → IIS/Blazor → MS SQL (Integrated Security, MSA);
легаси-таблицы ↔ `v2.*`; канал загрузки Excel (файл пользователя).

#### Контуры и классификация угроз STRIDE (сводка)

| Контур | S | T | R | I | D | E | Ключевые контроли (факт / план) |
|--------|---|---|---|---|---|---|----------------------------------|
| Blazor UI / circuit | x | x | | x | x | x | AD auth; PathBase в сайте легаси; kill switch write по домену (план) |
| Sync worker | | x | | x | x | | `V2SyncEnabled`; идемпотентный watermark; сверка; алерты (0.1.7.6+) |
| Excel import | x | x | | x | x | x | Content-type/сигнатура, anti-zip-bomb (план MVP 0.6); тот же CQRS |
| AD / роли | x | | x | x | | x | Read-only паритет с легаси до 0.4; доменные роли с 0.4; подтверждение ИБ 0.2.2.7 |
| Write-through | | x | x | x | x | x | Доменный write-toggle; LWW как в легаси; аудит с 1.0 |

*S* spoofing, *T* tampering, *R* repudiation, *I* information disclosure, *D* DoS, *E* elevation of privilege
(см. глоссарий §1.2а, STRIDE).

#### Открытые угрозы (residual risk)

| ID | Угроза | До закрытия | Owner | Ссылка |
|----|--------|-------------|-------|--------|
| T1 | Расширение прав относительно легаси на read-only | Письмо ИБ | Команда + ИБ | 0.2.2.7 / D-SEC-RO |
| T2 | Уязвимости зависимостей NuGet | SCA в CI | DevOps | 0.1.4.6 / D-SEC-SCA |
| T3 | Совмещение несовместимых ролей (SoD) | Матрица SoD | Архитектор + ИБ | приложение D-SEC-SOD |
| T4 | Отрицание привилегированного действия до field-audit | MVP 1.0 outbox | Команда | backlog 1.0.1 |
| T5 | Злонамеренный/повреждённый Excel | Контроли загрузки MVP 0.6 | Команда | 0.6.2.4 |
| T6 | Класс данных не зафиксирован (коммерческая тайна, персональные данные) | Классификация с владельцем продукта и ИБ | Команда + ИБ | этот пробел |

**Критерий готовности:** детальные карточки угроз по контурам; owner и Gate/MVP на открытых угрозах;
черновик согласован командой (до MVP 0.4); передан в ИБ с ролевой моделью (до MVP 0.5).

---

### Приложение D-SEC-SOD — разделение обязанностей (SoD)

> Статус: **черновик** (митигация D-SEC-SOD).  
> Матрица SoD — до MVP 0.4; иммутабельность журнала аудита — MVP 1.0.

#### Переходная модель доступа

| Фаза | Кто читает | Кто пишет | Примечание |
|------|------------|-----------|------------|
| MVP 0.1–0.3 | Пользователи с ≥1 ролью из `dbo.Role` | Никто в новом контуре | Паритет с легаси read-only; Windows Auth + `User`/`UserRole` |
| MVP 0.4+ | По доменным ролям | «Редактор {Домен}» и др. | Согласование с ИБ до MVP 0.5 |
| Админ | Глобальный суперюзер | Feature toggles, админка | Отдельный контроль |

Детали ролей — backlog 0.4.1 и §2 (строка «Роли»).

#### Матрица SoD (черновик)

| Роль A | Роль B | Совмещение | Обоснование |
|--------|--------|------------|-------------|
| Редактор {Домен X} | Редактор {Домен Y} | Допускается только явным решением | Снижение радиуса поражения (blast radius); кросс-домен — отдельное право |
| Редактор любого домена | Админ (toggles / URL Rewrite) | **Запрещено** по умолчанию | Разделение изменения данных и изменения контура |
| Оператор заявок | Админ | **Запрещено** | Workflow ≠ инфраструктурный контроль |
| Инициатор (только чтение) | Редактор | По AD-процессу заказчика | Как в легаси-процессах назначения групп |

> Заполнить фактическими AD-группами заказчика при согласовании MVP 0.4.

#### Привилегированные действия

| Действие | Кто | Аудит до MVP 1.0 | Аудит с MVP 1.0 |
|----------|-----|------------------|----------------|
| `V2SyncEnabled` on/off | Админ / поддержка | Лог приложения + изменение в `vw_FeatureToggle` | + field-level / outbox где применимо |
| `V2WriteEnabled_{Домен}` on/off | Админ / поддержка | То же | То же |
| IIS URL Rewrite (переключение UI) | Инфраструктура | Чеклист Gate B (2.7); изменение конфига IIS | Вне прикладного аудита — runbook |
| Ручной запуск reconciliation / сверки | Поддержка | Лог + алерт | То же |
| Назначение доменных ролей | Админ (админка 0.4) | Журнал админки (план раздела 9) | + неизменяемый след |
| Persist мастер-данных | Редактор | **Нет** field-audit (принятый риск) | Transactional Outbox, backlog 1.0.1 |

#### Неизменность журнала (решение — зафиксировать к MVP 1.0)

Варианты (выбрать один при проектировании раздела 16):

1. Журнал только на добавление (append-only) outbox + запрет UPDATE/DELETE правами приложения (MSA без DELETE на audit).
2. Периодический hash-chain / контрольная сумма суточных партиций.
3. Выгрузка в архив однократной записи (WORM) по политике ИБ (если потребует ИБ).

До выбора: риск «наивная асинхронная запись без durable-стадии» уже митигирован дизайном Transactional Outbox (§2).

**Критерий готовности:** матрица SoD с реальными AD-группами; запрещённые совмещения согласованы с ИБ;
выбран вариант иммутабельности журнала; ссылка из раздела 9.

---

### Приложение D-DATA-MIG — совместимые миграции схемы и откат

> Статус: **черновик** (митигация D-DATA-MIG).  
> Принять командой **до старта MVP 0.5** (пилот записи).

#### Контекст dual-run

На переходный период одновременно живут:

- легаси-таблицы (SoT до Gate C домена);
- схема `v2.*` (snapshot, `SyncState`, будущий outbox аудита);
- write-through из нового контура **в легаси** (с MVP 0.5).

Откат приложения ≠ откат данных. Restore БД — общий (легаси + `v2.*`) на одну точку времени
(§2, RTO/RPO).

#### Принципы расширения/сжатия схемы (expand/contract)

1. **Не ломать** читателей легаси и новый read-контур одним релизом.
2. Любое breaking-изменение колонки/таблицы — в два+ релиза: expand → migrate → contract.
3. Скрипты `SqlScripts/` идемпотентны или версионированы так, чтобы повторный накат на среду
   был безопасен (уже принятая практика publish SQL).
4. Изменения легаси-схемы, нужные только новому контуру, согласовывать отдельно: легаси остаётся
   SoT и обслуживает старый UI до Gate B/C.

#### Чеклист релиза read-only (текущая фаза)

- [ ] DDL только `v2.*` / programmability чтения
- [ ] Sync курсоры переживают рестарт (`SyncState`)
- [ ] Smoke после деплоя (2.4)
- [ ] Rollback приложения: предыдущий билд + SQL backward compatible

#### Чеклист релиза write (с MVP 0.5)

- [ ] Persist пишет в известную легаси-модель; нет скрытых DDL «на лету»
- [ ] Доменный write-toggle проверен (fail-closed)
- [ ] План отката: выключить `V2WriteEnabled_{Домен}` → пользователи на легаси-UI при необходимости
- [ ] Данные, уже записанные write-through, остаются в легаси (откат UI не требует rollback строк)
- [ ] Если менялся контракт идемпотентности — описан ключ повторного применения
- [ ] Миграции `v2.*`, связанные с outbox/аудитом — expand/contract

#### Запрещено без явного протокола архитектурного решения (ADR)

- Удаление/переименование колонок легаси, от которых зависит старый UI, до Gate D.
- Несовместимый с предыдущим билдом контракт snapshot без окна dual-version.
- «Откат данных» точечным DELETE после ошибочного батча без отчёта и процедуры (см. партиальный
  успех batch, 0.6.2.4).

**Критерий готовности:** принято командой до кодирования persist MVP 0.5; чеклист write включён
в релизный runbook; ссылка из раздела 10.

---

### Приложение D-OBS — индикаторы и цели уровня сервиса (SLI/SLO)

> Статус: **черновик** (митигация D-OBS).  
> Единый каркас поверх уже заданных числовых SLO sync. Закрыть таблицу до MVP 0.5.

Инфраструктурная доступность **98,5%** наследуется от легаси (общая ВМ/IIS/БД).  
Ниже — **прикладные** индикаторы уровня сервиса (SLI) и цели уровня сервиса (SLO) нового контура;
сценарии недоступности — §2.1.

#### Каталог SLI/SLO

| ID | SLI | SLO (цель) | Источник метрики | Алерт / реакция | Фаза |
|----|-----|------------|------------------|-----------------|------|
| S1 | Доступность UI нового контура (успешные circuit / HTTP) | В рамках общего 98,5% + учёт recycle | IIS / app logs | Релизное окно вне рабочего времени | 0.1+ |
| S2 | p95 ответа грида (пилот/тираж) | Не хуже легаси на сопоставимом объёме; ориентир секунды на 10 млн (факт бизнеса, reference §4.5) | App timing / SQL | Разбор запроса, индексы FTS | 0.1 / 0.2.1 |
| S3 | Lag курсора watermark (p95) | ≤ 5 мин (Gate A) | SyncState + логи воркера | 0.1.7.6 | 0.1+ |
| S4 | Успешность цикла сверки удалений | Цикл выполняется по расписанию | Лог reconciliation | 0.1.7.7 | до 0.2 |
| S5 | Ошибки persist (write) | Доля 5xx/SQL ошибок < порога (задать в MVP 0.5) | Лог CQRS | Доменный toggle / rollback UI | 0.5+ |
| S6 | Ошибки Excel import | Партиальный успех + отчёт; массовый fail → алерт | Лог Import | Runbook повторной загрузки | 0.6+ |
| S7 | Память / сессии Blazor | Нет деградации при пике ~100 (факт 0.1); порог алерта — задать | Perf counters | 0.1.7.8 / 0.1.7.11 | 0.1+ |

#### Уже подтверждённые факты (не путать с полным SLI-фреймворком)

- 100 сессий: ~3% CPU, ~1,5 МБ/circuit (MVP 0.1).
- Sync 200К: ~30 сек, SQL CPU ~15%.
- Бизнес: грид 10 млн — 0,5–3 сек (однопользовательский); выгрузка 120К — 2% CPU / 150 МБ.

Полный протокол нагрузки — приложение D-PERF / backlog 0.2.1.

Алерты S3/S4/S7 → существующий мониторинг (0.1.7.8). Без 0.1.7.8 контракт 98,5% формально
не исполним техподдержкой для новых сценариев §2.1.

**Критерий готовности:** пороги S5/S6/S7 численно зафиксированы; каждый SLI имеет дашборд/запрос
и владельца реакции.

---

### Приложение D-MET — метрики продуктивности пилотов

> Статус: **шаблон** (митигация D-MET / условие №7 architecture review; 0.0.5.4).  
> Первый обязательный замер — пилот MVP 0.5; далее 0.6 и первый домен 0.7.

Условие №7 ревью требует **4 показателя** на пилот. Сейчас есть «2 модели ИИ + факт» и пороги
дефектов; не хватает явной доли ревью и переделок (review/rework). Шаблон закрывает пробел без
единой «магической» цифры ускорения на всю программу (см. 0.0.5, D-EST).

#### Показатели (обязательные)

| # | Показатель | Как считать | Единица |
|---|------------|-------------|---------|
| 1 | Время человека | Суммарные часы разработчика на пилот (включая ревью и стабилизацию) | чел.-часы |
| 2 | Время агента | Оценка/лог сессий агента на генерацию и итерации (если нет лога — ретроспективная оценка, пометить) | часы |
| 3 | Review / rework | Доля времени человека на code review + переделки после замечаний / дефектов внутреннего теста от показателя 1 | % |
| 4 | Дефекты после приёмки | Critical/Blocker + Major, найденные после UAT/релиза пилота на горизонте стабилизационного окна | штук по severity |

Дополнительно (уже принято): оценка **двух моделей ИИ** до старта + **факт** после выполнения;
разброс моделей фиксировать.

#### Карточка пилота (копировать на каждый пилот)

```
Пилот: _______________  MVP: _____  Дата старта/финиша: _____
Категория работ (0.0.5): анализ / новый функционал / тираж / тест / ОПЭ

1. Время человека: ___ ч
2. Время агента: ___ ч (источник: лог / ретроспектива)
3. Review/rework: ___ %  ( ___ ч из показателя 1)
4. Дефекты после приёмки: Critical ___  Major ___  Other ___

Оценка модели A (без/с ИИ): ___ / ___
Оценка модели B (без/с ИИ): ___ / ___
Факт: ___
Отклонение факта от средней оценки моделей: ___

Примечания (что не ускоряется ИИ, блокеры, качество):
```

#### Правила интерпретации

- Не экстраполировать один пилот на всю таблицу этапов в [backlog.md](backlog.md).
- Не смешивать с точечными примерами бизнеса по отчётам (reference.md §4.5) — там эффект
  **архитектуры** без ИИ.
- ОПЭ/Gate (календарь) в показатель 2 не включать как «ускорение ИИ».

**Критерий готовности процесса:** карточки для MVP 0.5 и 0.6; после 3 категорий — вход в 0.0.5.3.

---

### Приложение D-PERF — протокол нагрузочного PoC 0.2.1

> Статус: **шаблон** (митигация D-PERF / условие №3 architecture review; [backlog.md](backlog.md) 0.2.1).  
> Шаблон (тип A); заполнение — по мере прогона (тип E).  
> **Блокер массового тиража 0.2.3:** минимум сценарий 0.2.1.10 (или явный принятый риск).

#### Метаданные прогона

| Поле | Значение |
|------|----------|
| Дата | |
| Среда | ДЕВ / ТЕСТ |
| Объём данных | (цель ~10 млн; факт клона) |
| Конфиг app | CPU / RAM / IIS |
| Конфиг SQL | CPU / RAM / edition |
| Исполнитель | |
| Версия билда | |

#### Предварительные факты (уже есть, не заменяют протокол)

| Факт | Результат | Ограничение |
|------|-----------|-------------|
| MVP 0.1: 100 сессий | ~3% CPU, ~1,5 МБ/circuit | Не ceiling |
| MVP 0.1: sync 200К | ~30 сек, SQL ~15% CPU | Не целевой объём |
| Бизнес: грид 10 млн | 0,5–3 сек | Однопользовательский |
| Бизнес: выгрузка 120К | 2% CPU / 150 МБ vs 15% / 1 ГБ легаси | Не 0.2.1.10 |

#### Чеклист сценариев

| ID | Сценарий | Статус (не начат / pass / fail / skip) | p95 / p99 / CPU / заметки |
|----|----------|----------------------------------------|---------------------------|
| 0.2.1.1 | Анализ логов легаси (профиль) | | |
| 0.2.1.2 | Тестовый объём ~10 млн | | |
| 0.2.1.3 | Конкурентное чтение | | |
| 0.2.1.4 | Холостой sync | | |
| 0.2.1.5 | Единичное изменение → snapshot | | |
| 0.2.1.6 | Batch 30 тыс. | | |
| 0.2.1.7 | Массовое изменение 100К | | |
| 0.2.1.8 | Fan-out родителя | | |
| 0.2.1.9 | Reconciliation IsArchive=1 | | |
| **0.2.1.10** | **Комбо: пик чтения + изменение + sync** | | |
| 0.2.1.11 | Сценарии 2–6 с sync on/off | | |
| 0.2.1.12 | CPU/IO/log/tempdb | | |
| 0.2.1.13 | Отказ под нагрузкой | | |
| 0.2.1.14 | Сверка с критериями приёмки | | |
| 0.2.1.15 | Часовой бэкап vs суточный | | |

#### Вердикт для тиража MVP 0.2

- [ ] 0.2.1.10 пройден в пределах критериев Gate A / протокола 0.2.1
- [ ] Либо: 0.2.1.10 не пройден, оформлен **принятый риск** с компенсирующими мерами и подписями
      (архитектор + владелец продукта) — тираж ограничен / отложен

**Решение:** разрешить 0.2.3 / отложить 0.2.3 / разрешить частично (указать группы).

Подписи: _____________ дата _____________

**Вложения:** ссылки на сырые логи, графики, скрипты нагрузки.

---

# II. Список работ (бэклог)

Канон чеклиста, статусы, таблица этапов и поэлементные оценки — **[backlog.md](backlog.md)**.
Нумерация пунктов: `{MVP}.{пакет}.{пункт}`. Не путать с номерами MVP и с `reference.md` §6.9
(условное архитектурное одобрение): нагрузочный PoC — пакет **0.2.1**.

Детальный расчёт — `docs/estimation/`
(`python docs/estimation/generate_effort_estimate.py`).
