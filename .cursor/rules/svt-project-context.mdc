---
description: Контекст проекта СВТ — домен, статус, дорожная карта
alwaysApply: true
---

# СВТ (Система Ведения Тарифов)

MDM-система логистических маршрутов, тарифов и складских ограничений. Переписывание легаси «коробочной» MDM (Angular + распределённый монолит) на **Blazor Server + MS SQL**.

## Масштаб домена

- 101 сущность в легаси; **63** в скоупе СВТ 2.0, **38** вне скоупа; 10–100 полей, до 2 млн записей (цель — 10 млн)
- Пакетная загрузка до 30 тыс. строк
- 300 читателей, 10 редакторов; 30 интеграционных потоков
- Состав доменов, работы (RO / RO+E / RO+E+L), меню и приоритеты — skill `svt-architecture` → `catalogs-scope.md` (источник истины по скоупу)

## Текущее состояние

Реализован **фреймворк справочников (read-only) и отчётов**. Примеры: TransportLeg, TransportRate, AverageRateLevel3, LocationsNodes, ParityRates (legacy: Legs, Rates). Есть зачаток Import (Excel → staging). Каркас **unit + integration** тестов в `tests/`. Значительная часть функционала отсутствует.

## Дорожная карта (MVP)

Порядок групп справочников: **Ставки → Маршруты → Продукты → Склады и ограничения → Матрица каналов → Интеграция с ЦБД → Вспомогательные**.

Пилот редактора/загрузчика TransportLegs (0.5/0.6) **не** равен первому Gate C (домен Ставки).

| MVP | Результат |
|-----|-----------|
| 0.0 | Исследование, архитектура, план, готовность сред |
| 0.1 | Пилот read-only «Ставки» + sync |
| 0.2 | 63 read-only + sync (5 пилотов уже на проде) |
| 0.3 | Модуль отчётов (~10), в т.ч. выгрузка конструктора отчётов ставок |
| 0.4 | Ролевая модель (ЕО упраздняется → редакторы доменов) |
| 0.5 | Пилот редактора «Транспортные плечи» (фреймворк, не Gate C) |
| 0.6 | Пилот загрузчика «Транспортные плечи» |
| 0.7 | Тираж записи 6 доменов (не всем E+L: см. реестр) + инкрементальный SoT flip |
| 0.8 | Вспомогательные (5 справочников) + последний SoT flip |
| 0.9 | Заявки на расчёт ставки (workflow; RO-грид уже в 0.2) |
| 1.0 | Аудит изменений |
| 1.1 | Дашборды (не конструктор отчётов ставок) |
| 1.2 | Оркестрация интеграций + отчёты; замена CurrencyRateActual на плановые курсы |
| 2.0 | Отключение легаси; затем схлопывание Вид/Тип/Тип 3 ур. транспорта |

Полный план и бэклог — skill `svt-architecture` → `roadmap.md`.

## Стратегия миграции

**Strangler Fig** — эволюционное замещение, общая БД на переходный период, параллельная работа систем. Не Big Bang. Обратная sync новая → легаси на переходный период.

## Структура репозитория

| Папка | Назначение |
|-------|------------|
| `Host/` | Точка входа, layout, маршруты, локализация |
| `Platform/` | Общий фреймворк: Grid, Reporting, UI, Infrastructure |
| `Modules/{Name}/` | Вертикальный срез справочника (List, Detail, Module) |
| `Import/` | Массовая загрузка Excel, валидаторы, staging |
| `SqlScripts/Platform/` | Схема v2, full-text, `GetBlazorGridData` |
| `SqlScripts/Modules/{Name}/` | DDL и SP конкретного справочника |
| `tests/BlazorSvt.UnitTests/` | Unit-тесты Platform, `GridQueryFactory`, contract DTO |
| `tests/BlazorSvt.IntegrationTests/` | Read-only integration-тесты SP/view на dev-БД |
| `docs/user-guide/` | Руководство пользователя СВТ 2.0 (RU/EN, Confluence Wiki Markup) |

Подробная архитектура — в skill `svt-architecture` и правиле `svt-architecture-principles`.
